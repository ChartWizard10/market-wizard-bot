# CAP-40B Change Note

CAP-40B attaches the CAP-40A pre-model boundary observation to existing Phase-14V traces for ranks 21–40 and adds an offline future-Daily outcome dataset builder.

The normal path creates no additional decision-trace rows: ranks 31–40 reuse existing near-cut traces and ranks 21–30 reuse their existing analysis-path trace. Ranks 41–60 remain ordinary near-cut telemetry.

The boundary outcome linker reuses VELOCITY-1D chronology and labels valid geometry without reconstructing a GPT-5.6 tier for unanalysed shadow candidates.

Production remains at 30 deep-analysis candidates. No additional GPT-5.6 call, tier authority, capital authority, routing change, universe change, cadence change, Discord change or real-4H authority change is introduced.
